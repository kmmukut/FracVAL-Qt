# Default results directory

The root `fracval.in` uses this directory by default. FracVAL creates the
selected `output_dir` automatically, so this folder can also be deleted and
recreated by a future run.

Each aggregate also writes a matching `.contacts.csv` sidecar containing the intended-contact overlap history.
